const sum = (n, m) => (
    n+m    // This is an implicit Arrow function where you don't need to return any statment in it it is implicit..
);
console.log(sum(5,4));

// Another implicit Function..

const cube = (i) => i*i*i;